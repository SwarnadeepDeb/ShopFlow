replicaCount: 1

image:
  repository: ACCOUNT_ID.dkr.ecr.us-east-1.amazonaws.com/shopflow/user-service
  tag: latest
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 3001

ingress:
  enabled: false
  annotations: {}
  hosts:
    - host: shopflow.example.com
      paths:
        - path: /api/users
          pathType: Prefix

# Non-sensitive config — stored in ConfigMap
config:
  PORT: "3001"
  DB_HOST: "postgres-host"
  DB_PORT: "5432"
  DB_NAME: "shopflow"
  DB_USER: "shopflow"
  NODE_ENV: "production"

# Sensitive config — stored in Secret (Vault in prod)
secret:
  DB_PASSWORD: "changeme"
  JWT_SECRET: "changeme-jwt-secret"

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 512Mi

hpa:
  enabled: true
  minReplicas: 1
  maxReplicas: 5
  targetCPUUtilizationPercentage: 70
  targetMemoryUtilizationPercentage: 80

probes:
  liveness:
    path: /health
    initialDelaySeconds: 30
    periodSeconds: 10
  readiness:
    path: /health
    initialDelaySeconds: 10
    periodSeconds: 5

serviceAccount:
  create: true
  name: ""
  annotations: {}

serviceMonitor:
  enabled: true
  interval: "30s"
  scrapeTimeout: "10s"

fullnameOverride: ""
